# Recomendado: token en base64 (evita problemas de saltos de línea)
GOOGLE_TOKEN_JSON_B64=

# Alternativa: token en JSON plano (string)
GOOGLE_TOKEN_JSON=

# Scopes esperados (informativo; el código usa defaults si no existe)
GMAIL_SCOPES=https://www.googleapis.com/auth/gmail.modify,https://www.googleapis.com/auth/gmail.send
